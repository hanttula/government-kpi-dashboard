/** @type {import('next').NextConfig} */
const nextConfig = {
  // For static export to HostGator: uncomment output: 'export' and set basePath if needed
  // output: 'export',
  images: {
    unoptimized: false,
  },
}

export default nextConfig
